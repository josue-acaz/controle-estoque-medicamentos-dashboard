import styled from "styled-components";

const PieGraphView = styled.div`
    height: 500px;
`;

const PieLabelGraph = styled.g``;

const PieLabelTitle = styled.text`
    font-size: 16px;
    color: #444444;
    font-weight: 600;
`;

const PieLabelText = styled.text`
    font-size: 14px;
    color: #999999;
`;

export {
    PieGraphView,
    PieLabelGraph,
    PieLabelTitle,
    PieLabelText,
};